import React from 'react';
import BinaryToggle from './BinaryToggle';

/** Same as BinaryToggle but semantically for 3 options. */
const TernaryToggle = (props) => <BinaryToggle {...props} />;

export default TernaryToggle;
